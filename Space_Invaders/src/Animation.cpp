#include <Animation.h>

Animation::Animation() {};

void Animation::Animate(sf::Texture objectTexture)//reference to a texture
{
	sf::Vector2u TextureSize = objectTexture.getSize();
	
};

void Animation::Update(sf::Time detlaTime)
{


};